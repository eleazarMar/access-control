{
  port:${PORT}

  db {
    host: ${DB_HOST}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    dbName: ${DB_NAME}
  }

  https {
    enabled: ${HTTPS_ENABLED}
    caPath:  ${HTTPS_CA}
    pemPath: ${HTTPS_PEM}
  }
}
